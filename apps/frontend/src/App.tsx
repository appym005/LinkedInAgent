export default function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">LinkedIn Agent</p>
        <h1>Frontend initialized.</h1>
        <p className="description">
          React and TypeScript are configured in the workspace. Replace this
          starter screen with the application UI.
        </p>
      </section>
    </main>
  );
}
